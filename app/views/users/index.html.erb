<% provide(:title, 'All users') %>
<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <h3>Follow artists to get their latest designs on your feeds.</h3>

    <%= will_paginate @users %>

    <ul class="users">
        <%= render @users %>
    </ul>

    <%= will_paginate @users %>
  </div>
</div>
